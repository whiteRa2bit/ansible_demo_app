---

app_repository: "git@git.vogres.tech:hse2021_assignments/whiterabbit_assignments/iac-assignment.git"
app_version: "solution"
app_root_path: "/opt/hello_webapp"
app_dest_path: "{{ app_root_path }}/app"
app_code_path: "{{ app_dest_path }}/app"
app_pages_path: "{{ app_code_path }}/pages.txt"
app_main: "app:app"
app_service_name: "hello_webapp"
app_num_workers: 4
app_socket: "127.0.0.1:5001"

app_run_as_user: "example_user"
deploy_key_file: "{{ app_root_path }}/deploy.key"

requirements_path: "{{ app_code_path }}/requirements.txt"
app_virtualenv_path: "{{ app_root_path }}/virtualenv"
app_virtualenv_bin_path: "{{ app_virtualenv_path }}/bin"
